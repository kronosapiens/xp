<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h3>
      From: <%= @lesson.admin.name %> (<%= @lesson.admin.email if @lesson.admin.email %>)
    </h3>
    <h3>
      Lesson Name: 
      <%= link_to @lesson.title, :controller => "lessons", :action => "show",
      :only_path => false, :id => @lesson.id, :host => "http://127.0.0.1:3000" %>
      <!-- when entering host URL, do NOT put a / at the end -->
    </h3>
    <p>
      <%= @content %>
    </p>

    <br><br>
    <p><em>
      You received this email because you signed up for a class on xp.
    </em></p>
  </body>
</html>