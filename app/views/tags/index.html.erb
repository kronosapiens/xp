<h2>Topics</h2>
<br>
  <% @topic_tags.each do |tag| %>
    <h4><%= link_to tag.name, tag_path(tag.slug) %></h4><br>
  <% end %>

  <br><br>

<h2>Locations</h2>
<br>
  <% @location_tags.each do |tag| %>
    <h4><%= link_to tag.name, tag_path(tag.slug) %></h4><br>
  <% end %>

  <br><br>

<h2>Times</h2>
<br>
  <% @time_tags.each do |tag| %>
    <h4><%= link_to tag.name, tag_path(tag.slug) %></h4><br>
  <% end %>